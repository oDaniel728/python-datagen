from datagen.utils.minecraft.identifier import Identifier
from datagen.utils.repr.villager_profession import VillagerProfession

class VillagerProfessions():
    CLERIC = VillagerProfession(Identifier.of('minecraft:cleric'))
    TOOLSMITH = VillagerProfession(Identifier.of('minecraft:toolsmith'))
    CARTOGRAPHER = VillagerProfession(Identifier.of('minecraft:cartographer'))
    WEAPONSMITH = VillagerProfession(Identifier.of('minecraft:weaponsmith'))
    LIBRARIAN = VillagerProfession(Identifier.of('minecraft:librarian'))
    BUTCHER = VillagerProfession(Identifier.of('minecraft:butcher'))
    FLETCHER = VillagerProfession(Identifier.of('minecraft:fletcher'))
    MASON = VillagerProfession(Identifier.of('minecraft:mason'))
    NONE = VillagerProfession(Identifier.of('minecraft:none'))
    FISHERMAN = VillagerProfession(Identifier.of('minecraft:fisherman'))
    LEATHERWORKER = VillagerProfession(Identifier.of('minecraft:leatherworker'))
    ARMORER = VillagerProfession(Identifier.of('minecraft:armorer'))
    SHEPHERD = VillagerProfession(Identifier.of('minecraft:shepherd'))
    NITWIT = VillagerProfession(Identifier.of('minecraft:nitwit'))
    FARMER = VillagerProfession(Identifier.of('minecraft:farmer'))
